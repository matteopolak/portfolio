---
import ProjectDemoLoading from './ProjectDemoLoading.astro';

interface Props {
  id: string;
  label: string;
  starter: string;
}

const { id, label, starter } = Astro.props;
---

<dialog
  class="code-demo-modal"
  data-project-demo={id}
  data-demo-state="loading"
  aria-label={`${label} playground`}
>
  <div class="code-demo-modal__panel" data-code-fullscreen-target>
    <div class="code-demo-modal__controls">
      <button type="button" data-code-fullscreen aria-label="Enter full screen" title="Full screen">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M4 9V4h5M15 4h5v5M20 15v5h-5M9 20H4v-5"></path>
        </svg>
      </button>
      <button type="button" data-code-close aria-label="Close playground" title="Close">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="m5 5 14 14M19 5 5 19"></path>
        </svg>
      </button>
    </div>

    <div class="code-playground__workspace">
      <div class="code-playground__source">
        <div class="code-editor__lines" data-code-lines aria-hidden="true">1</div>
        <div class="code-editor__surface">
          <pre class="code-editor__highlight" aria-hidden="true"><code data-code-highlight></code></pre>
          <textarea
            data-code-source
            data-project-demo-scroll
            aria-label={`${label} source code`}
            spellcheck="false"
            autocomplete="off">{starter}</textarea
          >
        </div>
        <button type="button" data-code-run title="Run (Ctrl/Command + Enter)">Run</button>
      </div>
      <section class="code-playground__output" aria-label="Program output">
        <pre data-code-output data-project-demo-scroll aria-live="polite">Run the program to see its output.</pre>
      </section>
    </div>
    <ProjectDemoLoading seed={id} />
  </div>
</dialog>

<style>
  .code-demo-modal {
    width: min(88vw, calc(86svh * 16 / 9), 88rem);
    max-width: none;
    height: fit-content;
    max-height: none;
    margin: 0;
    padding: 0;
    border: 0;
    outline: 0;
    overflow: visible;
    background: transparent;
    inset: 50% auto auto 50%;
    transform: translate(-50%, -50%);
  }

  .code-demo-modal[open] {
    animation: code-demo-enter 210ms cubic-bezier(0.2, 0.75, 0.25, 1) both;
  }

  .code-demo-modal[open][data-closing='true'] {
    animation: code-demo-exit 160ms ease-in both;
  }

  .code-demo-modal::backdrop {
    background: oklch(0% 0 0 / 0.3);
    backdrop-filter: blur(0.45rem);
    animation: code-backdrop-enter 210ms ease-out both;
  }

  .code-demo-modal[data-closing='true']::backdrop {
    animation: code-backdrop-exit 160ms ease-in both;
  }

  @keyframes code-demo-enter {
    from { opacity: 0; transform: translate(-50%, -48%) scale(0.975); }
    to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
  }

  @keyframes code-demo-exit {
    from { opacity: 1; transform: translate(-50%, -50%) scale(1); }
    to { opacity: 0; transform: translate(-50%, -48%) scale(0.985); }
  }

  @keyframes code-backdrop-enter { from { opacity: 0; } to { opacity: 1; } }
  @keyframes code-backdrop-exit { from { opacity: 1; } to { opacity: 0; } }

  .code-demo-modal__panel {
    position: relative;
    display: grid;
    width: 100%;
    min-width: 0;
    aspect-ratio: 16 / 9;
    overflow: visible;
    background: var(--paper);
  }

  .code-demo-modal__panel:fullscreen {
    width: 100vw;
    height: 100vh;
    aspect-ratio: auto;
  }

  .code-demo-modal__controls {
    position: absolute;
    top: 0;
    right: -3.4rem;
    z-index: 4;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    overflow: visible;
  }

  .code-demo-modal__controls button {
    display: grid;
    width: 2.65rem;
    height: 2.65rem;
    padding: 0;
    place-items: center;
    border: 1px solid oklch(100% 0 0 / 0.2);
    color: white;
    background: oklch(9% 0 0 / 0.78);
    box-shadow: 0 0.35rem 1.1rem oklch(0% 0 0 / 0.3);
    backdrop-filter: blur(0.45rem);
    cursor: pointer;
  }

  .code-demo-modal__controls svg {
    width: 1.25rem;
    height: 1.25rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: square;
    stroke-width: 2;
  }

  .code-demo-modal__controls button:hover {
    background: var(--blue);
  }

  .code-demo-modal__controls button:last-child:hover {
    background: var(--red);
  }

  .code-demo-modal__panel:fullscreen .code-demo-modal__controls {
    top: 0.75rem;
    right: 0.75rem;
    flex-direction: row;
  }

  .code-playground__workspace {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-width: 0;
    min-height: 0;
    overflow: hidden;
  }

  .code-playground__source,
  .code-playground__output {
    position: relative;
    min-width: 0;
    min-height: 0;
    margin: 0;
  }

  .code-playground__source {
    display: grid;
    grid-template-columns: 2.25rem minmax(0, 1fr);
    border-right: 1px solid oklch(20% 0 0 / 0.18);
    background: oklch(98% 0.01 91);
  }

  .code-playground__output {
    color: oklch(91% 0.01 91);
    background: #111317;
  }

  .code-editor__lines {
    height: 100%;
    padding: 0.8rem 0.42rem;
    overflow: hidden;
    border-right: 1px solid oklch(20% 0 0 / 0.08);
    color: oklch(42% 0 0 / 0.42);
    font: 400 clamp(0.72rem, 1.2vw, 0.88rem) / 1.55 ui-monospace, SFMono-Regular, Menlo, Monaco,
      Consolas, monospace;
    text-align: right;
    white-space: pre;
    user-select: none;
  }

  .code-editor__surface {
    position: relative;
    min-width: 0;
    min-height: 0;
  }

  .code-demo-modal textarea,
  .code-demo-modal pre {
    box-sizing: border-box;
    width: 100%;
    min-width: 0;
    min-height: 0;
    margin: 0;
    padding: 0.8rem;
    border: 0;
    outline: 0;
    overflow: auto;
    color: inherit;
    background: transparent;
    font: 500 clamp(0.72rem, 1.2vw, 0.88rem) / 1.55 ui-monospace, SFMono-Regular, Menlo, Monaco,
      Consolas, monospace;
    tab-size: 2;
    white-space: pre;
  }

  [data-project-demo-scroll] {
    overscroll-behavior: contain;
  }

  .code-demo-modal textarea {
    position: absolute;
    z-index: 1;
    inset: 0;
    display: block;
    height: 100%;
    padding-bottom: 4rem;
    color: transparent;
    caret-color: var(--ink);
    resize: none;
    scrollbar-width: none;
  }

  .code-demo-modal textarea::-webkit-scrollbar {
    display: none;
  }

  .code-editor__highlight {
    position: absolute;
    inset: 0;
    height: 100%;
    padding-bottom: 4rem;
    overflow: hidden;
    color: var(--ink);
    pointer-events: none;
  }

  .code-editor__highlight :global(.token.comment) {
    color: oklch(48% 0.03 145);
  }

  .code-editor__highlight :global(.token.keyword),
  .code-editor__highlight :global(.token.boolean) {
    color: var(--red);
    font-weight: 700;
  }

  .code-editor__highlight :global(.token.string),
  .code-editor__highlight :global(.token.char) {
    color: oklch(47% 0.15 145);
  }

  .code-editor__highlight :global(.token.number),
  .code-editor__highlight :global(.token.operator) {
    color: var(--blue);
  }

  .code-editor__highlight :global(.token.function) {
    color: oklch(48% 0.15 60);
  }

  .code-playground__output pre[data-stream='stderr'] {
    color: oklch(73% 0.19 28);
  }

  .code-playground__source > button {
    position: absolute;
    z-index: 2;
    right: 0.8rem;
    bottom: 0.8rem;
    min-width: 4.5rem;
    min-height: 2.4rem;
    padding: 0.45rem 0.85rem;
    border: 0;
    color: var(--paper);
    background: var(--red);
    font: inherit;
    font-size: 0.76rem;
    font-weight: 800;
    cursor: pointer;
  }

  .code-playground__source > button:hover {
    background: var(--blue);
  }

  .code-playground__source > button:disabled {
    opacity: 0.72;
    cursor: wait;
  }

  .code-playground__source > button[data-running] {
    color: transparent;
  }

  .code-playground__source > button[data-running]::after {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0.9rem;
    aspect-ratio: 1;
    border: 0.14rem solid oklch(98% 0.01 91 / 0.38);
    border-top-color: var(--paper);
    border-radius: 50%;
    content: '';
    animation: code-run-spinner 620ms linear infinite;
  }

  @keyframes code-run-spinner {
    from { transform: translate(-50%, -50%) rotate(0); }
    to { transform: translate(-50%, -50%) rotate(1turn); }
  }

  .code-demo-modal button:focus-visible {
    outline: 3px solid var(--yellow);
    outline-offset: 3px;
  }

  @media (max-width: 42rem) {
    .code-demo-modal {
      width: 96vw;
    }

    .code-demo-modal__panel {
      height: 82svh;
      aspect-ratio: auto;
    }

    .code-demo-modal__controls {
      top: -2.85rem;
      right: 0;
      flex-direction: row;
      gap: 0.35rem;
    }

    .code-demo-modal__controls button {
      width: 2.35rem;
      height: 2.35rem;
    }

    .code-playground__workspace {
      grid-template-columns: none;
      grid-template-rows: 1fr 1fr;
    }

    .code-playground__source {
      border-right: 0;
      border-bottom: 1px solid oklch(100% 0 0 / 0.18);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .code-demo-modal,
    .code-demo-modal::backdrop { animation: none !important; }

    .code-playground__source > button[data-running]::after { animation: none; }
  }
</style>
