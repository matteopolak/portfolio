<dialog class="project-demo-modal" data-project-demo="minecraft" aria-label="Minecraft browser demo">
  <div class="project-demo-modal__panel">
    <div class="project-demo-modal__actions">
      <button type="button" data-project-demo-fullscreen aria-label="Enter full screen" title="Full screen">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M4 9V4h5M15 4h5v5M20 15v5h-5M9 20H4v-5"></path>
        </svg>
      </button>
      <button type="button" data-project-demo-close aria-label="Close game" title="Close">
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="m5 5 14 14M19 5 5 19"></path>
        </svg>
      </button>
    </div>
    <div class="project-demo-modal__game" data-project-demo-game></div>
  </div>
</dialog>

<style>
  .project-demo-modal {
    width: min(88vw, calc(86svh * 16 / 9), 88rem);
    max-width: none;
    height: fit-content;
    max-height: none;
    margin: 0;
    padding: 0;
    border: 0;
    outline: 0;
    overflow: visible;
    background: transparent;
    inset: 50% auto auto 50%;
    transform: translate(-50%, -50%);
  }

  .project-demo-modal[open] {
    animation: project-demo-enter 210ms cubic-bezier(0.2, 0.75, 0.25, 1) both;
  }

  .project-demo-modal[open][data-closing='true'] {
    animation: project-demo-exit 160ms ease-in both;
  }

  .project-demo-modal::backdrop {
    background: oklch(0% 0 0 / 0.3);
    backdrop-filter: blur(0.45rem);
    animation: project-backdrop-enter 210ms ease-out both;
  }

  .project-demo-modal[data-closing='true']::backdrop {
    animation: project-backdrop-exit 160ms ease-in both;
  }

  @keyframes project-demo-enter {
    from { opacity: 0; transform: translate(-50%, -48%) scale(0.975); }
    to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
  }

  @keyframes project-demo-exit {
    from { opacity: 1; transform: translate(-50%, -50%) scale(1); }
    to { opacity: 0; transform: translate(-50%, -48%) scale(0.985); }
  }

  @keyframes project-backdrop-enter { from { opacity: 0; } to { opacity: 1; } }
  @keyframes project-backdrop-exit { from { opacity: 1; } to { opacity: 0; } }

  .project-demo-modal__panel {
    position: relative;
    width: 100%;
    min-width: 0;
    aspect-ratio: 16 / 9;
    background: #0a0a12;
  }

  .project-demo-modal__actions {
    position: absolute;
    top: 0;
    right: -3.4rem;
    z-index: 3;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .project-demo-modal button {
    display: grid;
    width: 2.65rem;
    height: 2.65rem;
    padding: 0;
    place-items: center;
    border: 1px solid oklch(100% 0 0 / 0.2);
    color: white;
    background: oklch(9% 0 0 / 0.78);
    box-shadow: 0 0.35rem 1.1rem oklch(0% 0 0 / 0.3);
    backdrop-filter: blur(0.45rem);
    cursor: pointer;
  }

  .project-demo-modal button svg {
    width: 1.25rem;
    height: 1.25rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: square;
    stroke-width: 2;
  }

  .project-demo-modal button:hover {
    background: var(--blue);
  }

  .project-demo-modal button:last-child:hover {
    background: var(--red);
  }

  .project-demo-modal button:focus-visible {
    outline: 3px solid var(--yellow);
    outline-offset: 3px;
  }

  .project-demo-modal__game {
    width: 100%;
    height: 100%;
    min-width: 0;
    min-height: 0;
    overflow: hidden;
    background: #0a0a12;
  }

  @media (max-width: 42rem) {
    .project-demo-modal {
      width: 96vw;
    }

    .project-demo-modal__actions {
      top: -2.85rem;
      right: 0;
      flex-direction: row;
      gap: 0.35rem;
    }

    .project-demo-modal button {
      width: 2.35rem;
      height: 2.35rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-demo-modal,
    .project-demo-modal::backdrop { animation: none !important; }
  }
</style>
