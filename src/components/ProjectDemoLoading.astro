<div class="project-demo-loading" data-project-demo-loading data-state="loading" aria-live="polite">
  <div class="project-demo-loading__mark" aria-hidden="true">M</div>
  <p data-project-demo-loading-label>Loading demo…</p>
  <div
    class="project-demo-loading__progress"
    data-project-demo-loading-progress
    role="progressbar"
    aria-label="Loading demo"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-valuenow="0"
  >
    <i data-project-demo-loading-bar></i>
  </div>
</div>

<style>
  .project-demo-loading {
    position: absolute;
    z-index: 3;
    inset: 0;
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 0.85rem;
    padding: 1.5rem;
    color: var(--ink);
    background: var(--paper);
    text-align: center;
    transition:
      opacity 180ms ease,
      visibility 180ms ease;
  }

  :global([data-project-demo][data-demo-state='ready']) .project-demo-loading {
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
  }

  .project-demo-loading__mark {
    position: relative;
    display: grid;
    width: 3.35rem;
    aspect-ratio: 1;
    place-items: center;
    overflow: hidden;
    color: color-mix(in oklch, var(--paper), transparent 68%);
    background: var(--ink);
    font-size: 1.4rem;
    font-weight: 900;
    line-height: 1;
  }

  .project-demo-loading__mark::before {
    --fill-top: calc(100% - var(--project-demo-fill, 0%));
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    color: transparent;
    background: linear-gradient(
      90deg,
      var(--red) 0 28%,
      var(--blue) 42% 68%,
      var(--yellow) 82% 100%
    );
    background-clip: text;
    background-size: 180% 100%;
    clip-path: polygon(
      0 calc(var(--fill-top) + 3%),
      22% calc(var(--fill-top) - 2%),
      48% calc(var(--fill-top) + 2%),
      73% calc(var(--fill-top) - 3%),
      100% calc(var(--fill-top) + 1%),
      100% 100%,
      0 100%
    );
    content: 'M';
    font: inherit;
    animation: project-demo-paint 1.5s ease-in-out infinite alternate;
    -webkit-background-clip: text;
  }

  @keyframes project-demo-paint {
    from {
      background-position: 0% 50%;
    }
    to {
      background-position: 100% 50%;
    }
  }

  .project-demo-loading p {
    min-height: 1.25em;
    margin: 0;
    color: color-mix(in oklch, var(--ink), transparent 28%);
    font-size: 0.8rem;
    font-weight: 700;
  }

  .project-demo-loading__progress {
    width: min(20rem, 62vw);
    height: 0.5rem;
    overflow: hidden;
    background: color-mix(in oklch, var(--ink), transparent 88%);
  }

  .project-demo-loading__progress i {
    display: block;
    width: 100%;
    height: 100%;
    background: var(--blue);
    transform: scaleX(var(--project-demo-progress, 0));
    transform-origin: left;
    transition: transform 160ms ease-out;
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__progress i {
    background: var(--red);
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__mark::before {
    background: var(--red);
  }

  @media (prefers-reduced-motion: reduce) {
    .project-demo-loading,
    .project-demo-loading__progress i {
      transition: none;
    }

    .project-demo-loading__mark::before {
      animation: none;
    }
  }
</style>
