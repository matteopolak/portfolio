<div class="project-demo-loading" data-project-demo-loading data-state="loading" aria-live="polite">
  <div
    class="project-demo-loading__tiles"
    data-project-demo-loading-progress
    role="progressbar"
    aria-label="Loading demo"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-valuenow="0"
  >
    <i class="project-demo-loading__tile project-demo-loading__tile--base" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--corner" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--disc" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--middle" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--cap" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--small-disc" data-project-demo-loading-tile></i>
    <i class="project-demo-loading__tile project-demo-loading__tile--top" data-project-demo-loading-tile></i>
  </div>
  <p data-project-demo-loading-label>Loading demo…</p>
</div>

<style>
  .project-demo-loading {
    position: absolute;
    z-index: 3;
    inset: 0;
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 0.85rem;
    padding: 1.5rem;
    color: var(--ink);
    background: var(--paper);
    text-align: center;
    transition:
      opacity 180ms ease,
      visibility 180ms ease;
  }

  :global([data-project-demo][data-demo-state='ready']) .project-demo-loading {
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
  }

  .project-demo-loading__tiles {
    display: grid;
    width: 5.4rem;
    height: 4.6rem;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(3, 1fr);
    gap: 0.2rem;
  }

  .project-demo-loading__tile {
    display: block;
    min-width: 0;
    min-height: 0;
    opacity: 0;
    transform: translateY(0.7rem) scale(0.82);
    transition:
      opacity 180ms ease,
      transform 260ms cubic-bezier(0.2, 0.85, 0.25, 1);
  }

  .project-demo-loading__tile[data-filled] {
    opacity: 1;
    transform: none;
    transition-delay: var(--tile-delay, 0ms);
  }

  .project-demo-loading__tile--base {
    --tile-delay: 0ms;
    grid-area: 3 / 1 / 4 / 3;
    background: var(--blue);
  }

  .project-demo-loading__tile--corner {
    --tile-delay: 35ms;
    grid-area: 3 / 3;
    background: var(--yellow);
  }

  .project-demo-loading__tile--disc {
    --tile-delay: 70ms;
    grid-area: 2 / 1;
    border-radius: 50%;
    background: var(--red);
  }

  .project-demo-loading__tile--middle {
    --tile-delay: 105ms;
    grid-area: 2 / 2 / 3 / 4;
    background: var(--ink);
  }

  .project-demo-loading__tile--cap {
    --tile-delay: 140ms;
    grid-area: 1 / 1;
    background: var(--yellow);
  }

  .project-demo-loading__tile--small-disc {
    --tile-delay: 175ms;
    grid-area: 1 / 2;
    border-radius: 50%;
    background: var(--blue);
  }

  .project-demo-loading__tile--top {
    --tile-delay: 210ms;
    grid-area: 1 / 3;
    background: var(--red);
  }

  .project-demo-loading p {
    min-height: 1.25em;
    margin: 0;
    color: color-mix(in oklch, var(--ink), transparent 28%);
    font-size: 0.8rem;
    font-weight: 700;
  }

  @media (prefers-reduced-motion: reduce) {
    .project-demo-loading,
    .project-demo-loading__tile {
      transition: none;
    }
  }
</style>
