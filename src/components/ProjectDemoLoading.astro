<div class="project-demo-loading" data-project-demo-loading data-state="loading" aria-live="polite">
  <div class="project-demo-loading__mark" aria-hidden="true">M</div>
  <p data-project-demo-loading-label>Loading demo…</p>
  <div
    class="project-demo-loading__progress"
    data-project-demo-loading-progress
    role="progressbar"
    aria-label="Loading demo"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-valuenow="0"
  >
    <i data-project-demo-loading-bar></i>
  </div>
</div>

<style>
  .project-demo-loading {
    position: absolute;
    z-index: 3;
    inset: 0;
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 0.85rem;
    padding: 1.5rem;
    color: var(--ink);
    background: var(--paper);
    text-align: center;
    transition:
      opacity 180ms ease,
      visibility 180ms ease;
  }

  :global([data-project-demo][data-demo-state='ready']) .project-demo-loading {
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
  }

  .project-demo-loading__mark {
    display: grid;
    width: 3.35rem;
    aspect-ratio: 1;
    place-items: center;
    color: var(--paper);
    background: var(--ink);
    font-size: 1.4rem;
    font-weight: 900;
    line-height: 1;
  }

  .project-demo-loading p {
    min-height: 1.25em;
    margin: 0;
    color: color-mix(in oklch, var(--ink), transparent 28%);
    font-size: 0.8rem;
    font-weight: 700;
  }

  .project-demo-loading__progress {
    width: min(20rem, 62vw);
    height: 0.5rem;
    overflow: hidden;
    background: color-mix(in oklch, var(--ink), transparent 88%);
  }

  .project-demo-loading__progress i {
    display: block;
    width: 100%;
    height: 100%;
    background: var(--blue);
    transform: scaleX(var(--project-demo-progress, 0));
    transform-origin: left;
    transition: transform 160ms ease-out;
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__progress i {
    background: var(--red);
  }

  @media (prefers-reduced-motion: reduce) {
    .project-demo-loading,
    .project-demo-loading__progress i {
      transition: none;
    }
  }
</style>
