<div class="project-demo-loading" data-project-demo-loading data-state="loading" aria-live="polite">
  <div class="project-demo-loading__mark" aria-hidden="true">
    <span class="project-demo-loading__mark-base">M</span>
    <span class="project-demo-loading__paint project-demo-loading__paint--red">M</span>
    <span class="project-demo-loading__paint project-demo-loading__paint--blue">M</span>
    <span class="project-demo-loading__paint project-demo-loading__paint--yellow">M</span>
  </div>
  <p data-project-demo-loading-label>Loading demo…</p>
  <div
    class="project-demo-loading__progress"
    data-project-demo-loading-progress
    role="progressbar"
    aria-label="Loading demo"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-valuenow="0"
  >
    <i data-project-demo-loading-bar></i>
  </div>
</div>

<style>
  .project-demo-loading {
    position: absolute;
    z-index: 3;
    inset: 0;
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 0.85rem;
    padding: 1.5rem;
    color: var(--ink);
    background: var(--paper);
    text-align: center;
    transition:
      opacity 180ms ease,
      visibility 180ms ease;
  }

  :global([data-project-demo][data-demo-state='ready']) .project-demo-loading {
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
  }

  .project-demo-loading__mark {
    position: relative;
    display: grid;
    width: 3.35rem;
    aspect-ratio: 1;
    place-items: center;
    overflow: hidden;
    color: color-mix(in oklch, var(--paper), transparent 68%);
    background: var(--ink);
    font-size: 1.4rem;
    font-weight: 900;
    line-height: 1;
  }

  .project-demo-loading__mark-base,
  .project-demo-loading__paint {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    font: inherit;
  }

  .project-demo-loading__paint {
    --fill-top: calc(100% - var(--paint-fill, 0%));
    clip-path: polygon(
      0 calc(var(--fill-top) + 3%),
      22% calc(var(--fill-top) - 2%),
      48% calc(var(--fill-top) + 2%),
      73% calc(var(--fill-top) - 3%),
      100% calc(var(--fill-top) + 1%),
      100% 100%,
      0 100%
    );
    animation: project-demo-wave 1.5s ease-in-out infinite alternate;
  }

  .project-demo-loading__paint--red {
    --paint-fill: var(--project-demo-fill-red, 0%);
    color: var(--red);
  }

  .project-demo-loading__paint--blue {
    --paint-fill: var(--project-demo-fill-blue, 0%);
    color: var(--blue);
    animation-delay: -0.55s;
  }

  .project-demo-loading__paint--yellow {
    --paint-fill: var(--project-demo-fill-yellow, 0%);
    color: var(--yellow);
    animation-delay: -1.05s;
  }

  @keyframes project-demo-wave {
    from {
      clip-path: polygon(
        0 calc(var(--fill-top) + 3%),
        22% calc(var(--fill-top) - 2%),
        48% calc(var(--fill-top) + 2%),
        73% calc(var(--fill-top) - 3%),
        100% calc(var(--fill-top) + 1%),
        100% 100%,
        0 100%
      );
    }
    to {
      clip-path: polygon(
        0 calc(var(--fill-top) - 2%),
        22% calc(var(--fill-top) + 2%),
        48% calc(var(--fill-top) - 3%),
        73% calc(var(--fill-top) + 2%),
        100% calc(var(--fill-top) - 1%),
        100% 100%,
        0 100%
      );
    }
  }

  .project-demo-loading p {
    min-height: 1.25em;
    margin: 0;
    color: color-mix(in oklch, var(--ink), transparent 28%);
    font-size: 0.8rem;
    font-weight: 700;
  }

  .project-demo-loading__progress {
    width: min(20rem, 62vw);
    height: 0.5rem;
    overflow: hidden;
    background: color-mix(in oklch, var(--ink), transparent 88%);
  }

  .project-demo-loading__progress i {
    display: block;
    width: 100%;
    height: 100%;
    background: var(--blue);
    transform: scaleX(var(--project-demo-progress, 0));
    transform-origin: left;
    transition: transform 160ms ease-out;
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__progress i {
    background: var(--red);
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__mark-base {
    color: var(--red);
  }

  .project-demo-loading[data-state='error'] .project-demo-loading__paint {
    display: none;
  }

  @media (prefers-reduced-motion: reduce) {
    .project-demo-loading,
    .project-demo-loading__progress i,
    .project-demo-loading__paint {
      transition: none;
      animation: none;
    }

  }
</style>
